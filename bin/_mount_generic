mkdir -p "$local_dir"
umount "$local_dir"

# add --vv for more info

rclone mount \
--vfs-cache-mode full \
--allow-other \
--sftp-key-use-agent \
--sftp-host "$host" \
--sftp-user "$user" \
:sftp:"$remote_dir" \
"$local_dir"
