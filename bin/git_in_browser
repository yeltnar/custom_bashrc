#!/bin/bash

if [ -z $browser ]; then
    browser='firefox'
fi

git_remote=$(git remote -v | head -n1 | awk '{print $2}')
http_remote=$(echo $git_remote | awk -F '[@:/]' '{print "https://" $2 "/" $3 "/" $4}')
$browser $http_remote
