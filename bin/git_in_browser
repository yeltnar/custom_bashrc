#!/bin/bash

if [ -z $browser ]; then
    browser='firefox'
fi

export git_remote=$(git remote -v | head -n1 | awk '{print $2}')
http_remote=$(_git_to_http)
git_remote=$(_http_to_git)

$browser $http_remote
