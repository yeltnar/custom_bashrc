umount /tmp/do
mkdir -p /tmp/do
# rclone mount --vfs-cache-mode full --allow-other exposed-pi:/media/pi /tmp/pi

rclone mount \
--vfs-cache-mode full \
--allow-other \
--sftp-host do.andbrant.com \
--sftp-user pi \
:sftp:/root \
/tmp/do
