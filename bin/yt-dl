# consider -x for audio and --add-metadata
# the directory should have the g+w flag for this to work
echo "consider -x for audio and --add-metadata"
podman run \
--rm -it \
-v "$(pwd)":/workdir:rw \
mikenye/youtube-dl "$@"
# -e PGID=$(id -g) \
# -e PUID=$(id -u) \
