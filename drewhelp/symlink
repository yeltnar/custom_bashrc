 # create symlink to a file or dir
 ln -s source_file myfile

# replace symlink with new file
ln -sf /new/target /path/to/symlink

# replace symlink with new directory or file 
ln -sfn /new/target /path/to/symlink

