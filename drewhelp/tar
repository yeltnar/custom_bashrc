# compress archive-name from source
tar -zcvf archive-name.tar.gz source-directory-name
# same, but will compress contents of -C without full path
tar -zcvf gpg_dir.tar.gz -C /tmp/gpg_home3/ .

# extract tar (to current directory)
tar -zxvf prog-1-jan-2005.tar.gz
# same, but will extract contents to /tmp/rm/
tar -zxvf /tmp/gpg_dir.tar.gz -C /tmp/rm/

# extract tar to output directory 
tar xf file.tar -C /path/to/directory

-C changes working dir for process. This changes how paths work when compressing

