# start default network... seems this is needed after reboot (when no auto start is set up)
sudo virsh net-start --network default 

# stop (alot) of the service 
sudo systemctl stop libvirtd.service 
