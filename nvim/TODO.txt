# file search depends on ripgrep (rg) to be installed

# auto run this
git clone --depth 1 https://github.com/wbthomason/packer.nvim ~/.local/share/nvim/site/pack/packer/start/packer.nvim

# after linking, and cloning this repo, need to 
# `:so` while having the packer.lua file open
# `:PackerSync`

# cab `find ~` or `tree ~` and remove all dotfiles for nvim to re-install
# help https://youtubetranscript.com/?v=w7i4amO_zaE&t=166

